# -*- coding: utf-8 -*-
"""Diabetes Prediction using Machine Learning.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1FDZUzH3PRF94aWwLI4zNW87NPEpern4g

Import Dependices
"""

import numpy as np
import pandas as pd
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split
from sklearn import svm
from sklearn.metrics import accuracy_score

"""Data Collection and Anlaysis
PIMA Diabetes Datasets
"""

# loading the diabetes dataset to pandas fram
diabetes_datasets = pd.read_csv("/content/diabetes.csv")

pd.read_csv?

#printing first 5 rows of tha datasets
diabetes_datasets.head()

# numbers of rows and columns in this dataset
diabetes_datasets.shape

# getting the statistical methos of the data
diabetes_datasets.describe()

diabetes_datasets['Outcome'].value_counts()

"""

*   0 --> Non Diabetic
*   1 --> Diabeticem

"""

diabetes_datasets.groupby('Outcome').mean()

# Seperating data and lables
X = diabetes_datasets.drop(columns = 'Outcome', axis=1) #axis=1 we droping column so 1 in row means 0
y = diabetes_datasets['Outcome']

print(X)
print(y)

"""Data Standardization"""

scaler = StandardScaler()

scaler.fit(X)

standardizrd_data = scaler.transform(X)

print(standardizrd_data) #all values from ony 0 to 1... it helps to make prediction easily

X = standardizrd_data
y = diabetes_datasets['Outcome']

print(X)
print(y)

"""Train Test Split"""

X_train, X_test, y_train, y_test = (
    train_test_split(X, y, test_size=0.2, stratify=y, random_state=2)
)

print(X.shape, X_train.shape, X_test.shape)

"""Training the Model"""

classifier = svm.SVC(kernel='linear') #suport vector classifier

# training the support vector machine Classifier
classifier.fit(X_train, y_train)

"""Model evaluation"""

#finding accuracy score on training data
X_train_prediction = classifier.predict(X_train)
training_data_accuracy = accuracy_score(X_train_prediction, y_train) # we have using trained machine learing model

print('Accuracy score of the training data : ', training_data_accuracy)

#findin accuracy score in testing data
X_test_prediction = classifier.predict(X_test)
testing_data_accuracy = accuracy_score(X_test_prediction, y_test)

print("Accuracy score of the testing data : ", testing_data_accuracy)

"""Making a predictive system"""

input_data = (3, 169,	74,	19,	125,	29.9,	0.268,	31)

#changing the imput data to numpy array
input_data_as_numpy_array = np.asarray(input_data)

#reshaping the array as we are predicting for one instance
input_data_reshaped = input_data_as_numpy_array.reshape(1, -1)

# standradize the input data
std_data = scaler.transform(input_data_reshaped)
print(std_data)

prediction = classifier.predict(std_data)
print(prediction)

if (prediction[0] == 0):
  print('The person is not diabetic')
else:
  print('The person is diabetic')

